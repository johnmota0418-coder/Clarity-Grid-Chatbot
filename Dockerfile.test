FROM python:3.11-slim

WORKDIR /app

# Install only FastAPI and uvicorn for testing
RUN pip install fastapi uvicorn

COPY test_app.py .

CMD ["sh", "-c", "uvicorn test_app:app --host 0.0.0.0 --port ${PORT:-8000}"]